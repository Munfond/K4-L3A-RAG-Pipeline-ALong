# Individual contribution report

---

## Thông tin

- Họ và tên: Nguyễn Đức Anh
- Mã học viên: 2A202602625
- Nhóm: ALong
- Repository/branch: Munfond/K4-L3A-RAG-Pipeline-ALong/main

## Phần việc đã thực hiện

| Module/deliverable | Việc tôi trực tiếp làm | File/commit/PR | Trạng thái |
|---|---|---|---|
| Task 1 — Thu thập tài liệu | Tìm kiếm, chọn lọc và nạp 3 tài liệu quy chế, đề thi IELTS 2026 vào thư mục legal | `src/task1_collect_legal_docs.py`, `data/landing/legal/` | Done |
| Task 2 — Crawl tin tức | Triển khai crawler bằng Crawl4AI thu thập 5 bài viết chuyên sâu IELTS Writing từ IDP | `src/task2_crawl_news.py`, `data/landing/news/` | Done |
| Task 3 — Chuẩn hóa Markdown | Viết logic convert DOCX/JSON sang chuẩn Markdown kèm header metadata | `src/task3_convert_markdown.py`, `data/standardized/` | Done |

## Quyết định kỹ thuật quan trọng

1. **Quyết định:** Sử dụng Crawl4AI kết hợp Playwright headless Chromium để crawl bài viết từ IDP Vietnam.  
   **Lý do/evidence:** Trang tin của IDP chứa các thành phần động và metadata phong phú; Crawl4AI tự động trích xuất cấu trúc Markdown sạch, lọc bỏ quảng cáo và menu điều hướng.  
   **Trade-off:** Cần cài đặt thêm Playwright browser binaries khiến thời gian setup ban đầu lâu hơn requests thông thường.

2. **Quyết định:** Xây dựng module trích xuất DOCX thuần Python bằng `zipfile` và `xml.etree` làm cơ chế fallback cho Task 3.  
   **Lý do/evidence:** Tránh việc pipeline bị gián đoạn do lỗi `MissingDependencyException` khi môi trường chưa cài đặt gói `markitdown[docx]`.  
   **Trade-off:** Chỉ trích xuất được văn bản thô dạng đoạn văn, không giữ nguyên 100% định dạng bảng phức tạp như MS Word chuyên dụng.

## Kiểm thử và kết quả

- Test hoặc query tôi đã dùng:
  - `pytest tests/test_acceptance.py -k test_corpus_has_required_legal_documents -v`
  - `pytest tests/test_acceptance.py -k test_corpus_has_required_news_with_metadata -v`
  - `pytest tests/test_acceptance.py -k test_standardized_output_covers_both_source_types -v`
- Kết quả trước/sau nếu có: Sau khi chạy Task 3, toàn bộ 3 file tài liệu legal và 5 file news đều được chuẩn hóa sang `.md` với độ dài nội dung > 200 ký tự.
- Lỗi đã phát hiện và cách xử lý: Khắc phục lỗi thiếu dependency DOCX của MarkItDown bằng phương pháp phân tích OpenXML trực tiếp từ tệp nén.

## Điều còn hạn chế

- Một hạn chế cụ thể của phần tôi làm: Tốc độ crawl 5 bài viết mất khoảng 8-12 giây do crawl tuần tự.
- Nếu có thêm thời gian, thay đổi đầu tiên tôi sẽ thực hiện: Bổ sung `asyncio.gather` để crawl song song nhiều URL cùng lúc và thêm cơ chế tự động dọn dẹp các đường link hình ảnh bị hỏng trong Markdown.

## Xác nhận đóng góp

Tôi xác nhận nội dung trên phản ánh đúng phần việc của mình và có thể giải thích hoặc chạy lại trong buổi demo.

- Ngày: 20/09/2026
- Tên thành viên: Nguyễn Đức Anh
